{
  "name": "artwithheartandgifts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wix dev",
    "dev:full": "concurrently \"npm run dev\" \"npm run backend\"",
    "build": "echo 'Building Wix site...'",
    "preview": "wix preview",
    "publish": "wix publish",
    "deploy": "node scripts/deploy-to-wix.mjs",
    "deploy:auto": "node scripts/auto-deploy.mjs",
    "test": "npm run build",
    "clean:system": "powershell -File scripts/clean-system.ps1 -RemoveVite -CleanNpmCache",
    "setup:backend": "cd backend && npm install",
    "backend": "cd backend && npm start",
    "admin": "echo 'Admin functionality available through localStorage in browser console'"
  },
  "devDependencies": {
    "jsdom": "^22.1.0",
    "puppeteer": "^24.19.0",
    "sharp": "^0.32.5",
    "vite": "^7.1.2"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "concurrently": "^8.2.2",
    "docx": "^9.5.1",
    "dotenv": "^17.2.2",
    "googleapis": "^159.0.0",
    "xlsx": "^0.18.5"
  }
}
